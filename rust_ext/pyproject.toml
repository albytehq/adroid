[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "adroid-rust"
version = "0.3.5"
description = "Rust extensions for Adroid — fast uiautomator XML parser"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
